<h2><%= @category.name %></h2>
  <table>
    <tr>
      <th>
        Name
      </th>
      <th>
        pixel_value
      </th>
      <th>
        hex_value
      </th>
      <th>
        options
      </th>
    </tr>
    <% @category.properties.each do |property| %>
      <tr>
        <td>
          <%= property.name %>
        </td>
        <td>
          <%= property.pixel_value %>
        </td>
        <td>
          <%= property.hex_value %>
        </td>
        <td>
          <%= link_to 'edit', edit_category_property_path(@category, property) %>
          <%= link_to 'delete', category_property_path(@category, property), method: :delete,
                                data: { confirm: 'Are you sure you want to delete?' } %>
        </td>
      </tr>
    <% end %>
  </table>
<%= link_to 'New Property', new_category_property_path(@category) %>
