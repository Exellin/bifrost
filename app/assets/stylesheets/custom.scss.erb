@import "bootstrap";

.site-container {
  position: relative;
  top: <%=Property.find_by_name("nav_height").pixel_value%>px;
}

table {
    border-collapse: collapse;
    width: 100%;
}

td, th {
    border: 1px solid black;
    text-align: left;
    padding: 10px;
}